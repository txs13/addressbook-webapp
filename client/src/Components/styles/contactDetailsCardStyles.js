import { makeStyles } from "@mui/styles";

const useContactDetailsCardStyles = makeStyles((theme)=>({
    contactCardContainer: {
        padding: '5px',
            
    }, 
    inputCard: {
        padding: '5px',
        
    },
    inputBox: {
        width:"100%",
        padding: "7px",
        
    }

}))

export default useContactDetailsCardStyles