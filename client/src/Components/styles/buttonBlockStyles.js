import {makeStyles} from '@mui/styles'

const useButtonBlockStyles = makeStyles((theme)=>({
    buttonBlock: {
        marginTop: "15px",
        marginBottom: "5px",
    }
}))

export default useButtonBlockStyles